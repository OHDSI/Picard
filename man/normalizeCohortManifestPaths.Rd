% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest_helpers.R
\name{normalizeCohortManifestPaths}
\alias{normalizeCohortManifestPaths}
\title{Normalize Legacy Cohort Manifest File Paths}
\usage{
normalizeCohortManifestPaths(
  cohortsFolderPath = here::here("inputs/cohorts"),
  dryRun = FALSE
)
}
\arguments{
\item{cohortsFolderPath}{Character. Path to the cohorts folder (or anywhere
inside the study repository). Defaults to \code{here::here("inputs/cohorts")}.}

\item{dryRun}{Logical. When \code{TRUE}, report what would change without writing.
Defaults to \code{FALSE}.}
}
\value{
Invisibly, a tibble with columns \code{id}, \code{old_path}, \code{new_path},
\code{status} (one of \code{"rewritten"}, \code{"would_rewrite"}, \code{"unchanged"},
\code{"broken"}, \code{"no_path"}).
}
\description{
A one-time cleanup that rewrites the \code{file_path} of every active/stale row in
\code{cohortManifest.sqlite} so it is stored \strong{relative to the study repository
root} (the current convention). Manifests created before this convention may
hold working-directory-relative, manifest-folder-relative, or absolute paths;
those still load through a compatibility resolver, but normalizing makes the
stored values stable and portable across machines and working directories.
}
\details{
Only \code{file_path} is changed. Content hashes, \code{status}, timestamps and the
change-detection metadata are left untouched, so running this never marks a
cohort stale or shows up as a definition change in syncManifest.

Ordinary loads never mutate the SQLite file — this is the \emph{only} operation
that rewrites stored paths, and you run it explicitly.
}
\seealso{
\code{\link[=findStudyProjectRoot]{findStudyProjectRoot()}}, \code{\link[=loadCohortManifest]{loadCohortManifest()}}
}
