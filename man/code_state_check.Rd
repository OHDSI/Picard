% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{code_state_check}
\alias{code_state_check}
\title{Run the Code State Pre-flight Check}
\usage{
code_state_check(
  testMode = FALSE,
  skipCodeStateCheck = FALSE,
  ignoreUncommittedPaths = character(0)
)
}
\arguments{
\item{testMode}{Logical. TRUE when running the pipeline in test mode.}

\item{skipCodeStateCheck}{Logical. TRUE to skip the check entirely.}

\item{ignoreUncommittedPaths}{Character vector of repo-relative paths whose
uncommitted changes do not fail the check.}
}
\value{
A list with \code{status} ("pass", "warn", "fail" or "skip"),
\code{message} for the checklist line, and \code{codeState}.
}
\description{
Resolves the "Code state" pre-flight check into a checklist entry
plus a \code{codeState} provenance record that downstream code writes to the run
history. Split out of \code{\link[=runPreflightChecks]{runPreflightChecks()}} so the escape-hatch logic is
testable on its own.

A run that passes only because changes were ignored, or because the check
was skipped, is reported as a \strong{warning} rather than a pass or a silent
skip — an escape hatch that hides itself is worse than no escape hatch.
}
\keyword{internal}
