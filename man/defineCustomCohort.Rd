% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest_cohorts.R
\name{defineCustomCohort}
\alias{defineCustomCohort}
\title{Define (Enrich) a Custom SQL Cohort in the Manifest}
\usage{
defineCustomCohort(
  manifest,
  label,
  tags = list(),
  cohortId = NULL,
  sqlFilePath = NULL
)
}
\arguments{
\item{manifest}{A \link{CohortManifest} R6 object returned by \code{\link[=loadCohortManifest]{loadCohortManifest()}}.}

\item{label}{Character. The user-friendly display name for the cohort
(e.g., \code{"High-dose corticosteroid initiators"}).}

\item{tags}{Named list. Optional metadata tags (e.g., \code{list(category = "Exposure")}).
Defaults to \code{list()}.}

\item{cohortId}{Integer. The cohort ID assigned in the manifest. Provide either
\code{cohortId} or \code{sqlFilePath}, not both.}

\item{sqlFilePath}{Character. Path to the SQL file (relative or absolute). Provide
either \code{sqlFilePath} or \code{cohortId}, not both.}
}
\value{
Invisibly returns \code{NULL}. Prints status messages on success.
}
\description{
Enriches a cohort that has already been discovered by \code{\link[=loadCohortManifest]{loadCohortManifest()}} with
a user-friendly label, tags, and the \code{"custom"} cohort type. Updates both the
in-memory manifest and the SQLite database so the enrichment persists across sessions.
}
\details{
\strong{Workflow:}
\enumerate{
\item Place your custom SQL file in \verb{inputs/cohorts/sql/}
\item Call \code{\link[=loadCohortManifest]{loadCohortManifest()}} — the file is auto-discovered with \code{cohortType = "circe"}
and the filename as its label
\item Call \code{defineCustomCohort()} to give it a proper label, tags, and mark it as \code{"custom"}
\item Subsequent \code{\link[=loadCohortManifest]{loadCohortManifest()}} calls restore the label/tags/type from the database
}

\strong{SQL requirements:}
Your SQL must use SqlRender parameters instead of hardcoded values. The following
parameters are automatically injected by \link{CohortManifest}\verb{$generateCohorts()}:
\itemize{
\item \verb{@target_cohort_id} — the cohort definition ID assigned by the manifest
\item \verb{@target_database_schema} — the schema where the cohort table resides
\item \verb{@target_cohort_table} — the cohort table name
\item \verb{@cdm_database_schema} — the CDM schema (for referencing OMOP clinical tables)
}

A DELETE step before the INSERT is strongly recommended for idempotency:

\if{html}{\out{<div class="sourceCode sql">}}\preformatted{DELETE FROM @target_database_schema.@target_cohort_table
  WHERE cohort_definition_id = @target_cohort_id;
}\if{html}{\out{</div>}}

SQL portability warnings are automatically shown when you call this function and
again at execution time.
}
\examples{
\dontrun{
  # Step 1: place my_cohort.sql in inputs/cohorts/sql/
  # Step 2: load manifest
  manifest <- loadCohortManifest()

  # Step 3: enrich the cohort by file path
  defineCustomCohort(
    manifest = manifest,
    sqlFilePath = "inputs/cohorts/sql/my_cohort.sql",
    label = "My Custom Cohort",
    tags = list(category = "Exposure", subCategory = "Corticosteroids")
  )

  # Or enrich by cohort ID
  defineCustomCohort(
    manifest = manifest,
    cohortId = 5L,
    label = "My Custom Cohort",
    tags = list(category = "Exposure")
  )
}
}
