% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/buildDependentCohorts.R
\name{buildStratifiedCohorts}
\alias{buildStratifiedCohorts}
\title{Split a Base Cohort into Multiple Stratified Sub-Cohorts}
\usage{
buildStratifiedCohorts(
  baseCohortId,
  strata,
  labelPrefix = NULL,
  cohortsFolder = here::here("inputs/cohorts"),
  manifest
)
}
\arguments{
\item{baseCohortId}{Integer. The cohort definition ID to split.}

\item{strata}{Named list. Each element is either a named list of demographic
filters or a character string SQL WHERE condition. Names become cohort
labels (optionally prefixed by \code{labelPrefix}).}

\item{labelPrefix}{Character or \code{NULL}. If provided, prepended to each
stratum name with a \code{-} separator (e.g. \code{"CKD"} + \code{"Male"} →
\code{"CKD - Male"}).}

\item{cohortsFolder}{Character. Path to inputs/cohorts/. Defaults to
\code{here::here("inputs/cohorts")}.}

\item{manifest}{A \code{CohortManifest} object. Required. Cohort IDs are
auto-assigned from the next available manifest ID — never supply them
manually.}
}
\value{
Invisibly returns a named list of \code{CohortDef} objects, one per
stratum including Unclassified. The list is keyed by the full cohort label.
}
\description{
Splits a single base cohort into N named stratum cohorts plus an automatic
\strong{Unclassified} cohort containing subjects that match none of the named
strata. Each stratum is registered as a separate entry in the manifest with
an auto-assigned ID and \code{cohortType = "subset"}. A single SQL file is written
per stratum so \code{generateCohorts()} processes them independently.
}
\details{
Strata can be defined in two ways and may be mixed in the same call:

\strong{Demographic (named list):}

\if{html}{\out{<div class="sourceCode r">}}\preformatted{list(
  "Male"   = list(genderConceptIds = 8507),
  "Female" = list(genderConceptIds = 8532),
  "65+"    = list(minAge = 65)
)
}\if{html}{\out{</div>}}

Supported keys: \code{genderConceptIds}, \code{raceConceptIds}, \code{ethnicityConceptIds},
\code{minAge}, \code{maxAge}. Multiple keys within one stratum are \code{AND}-ed.

\strong{Custom SQL WHERE clause (character string):}

\if{html}{\out{<div class="sourceCode r">}}\preformatted{list(
  "West"  = "p.location_id IN (1, 4, 5, 6, 12)",
  "South" = "p.location_id IN (2, 3, 8, 9, 10)"
)
}\if{html}{\out{</div>}}

The expression may reference \code{bc} (cohort table alias) and \code{p} (person
table alias).

An \strong{Unclassified} stratum is always appended automatically. Its WHERE
condition is the logical negation of every named stratum combined with
\verb{AND NOT (...)}, ensuring every subject in the base cohort appears in exactly
one output cohort.
}
