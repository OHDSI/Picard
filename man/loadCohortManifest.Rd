% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest_helpers.R
\name{loadCohortManifest}
\alias{loadCohortManifest}
\title{Load Cohort Manifest from SQLite Database}
\usage{
loadCohortManifest(
  cohortsFolderPath = here::here("inputs/cohorts"),
  executionSettings = NULL,
  verbose = TRUE
)
}
\arguments{
\item{cohortsFolderPath}{Character. Path to the cohorts folder containing the manifest
database. Defaults to \code{here::here("inputs/cohorts")}.}

\item{executionSettings}{An ExecutionSettings object containing database configuration
for cohort generation. Optional; can be added later using \verb{$setExecutionSettings()}.}

\item{verbose}{Logical. If TRUE, prints informative messages. Defaults to TRUE.}
}
\value{
A CohortManifest R6 object.
}
\description{
Loads a CohortManifest R6 object from an existing \code{cohortManifest.sqlite} database.
This is a pure read from SQLite — it does not scan directories or auto-add new files.
If new files exist on disk that aren't in the manifest, a warning is printed
suggesting the appropriate \verb{$add*()} method.
}
\details{
If no SQLite database exists at the expected path, the function stops with an
error directing the user to \code{initCohortManifest()}.

After loading, the function checks for new files in \verb{json/}, \verb{sql/}, and \verb{derived/}
directories that are not tracked in the manifest. These are reported as warnings
but NOT auto-added (because \code{category} is required and cannot be guessed).
}
