% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest_helpers.R
\name{normalizeConceptSetManifestPaths}
\alias{normalizeConceptSetManifestPaths}
\title{Normalize Legacy Concept Set Manifest File Paths}
\usage{
normalizeConceptSetManifestPaths(
  conceptSetsFolderPath = here::here("inputs/conceptSets"),
  dryRun = FALSE
)
}
\arguments{
\item{conceptSetsFolderPath}{Character. Path to the conceptSets folder (or
anywhere inside the study repository). Defaults to
\code{here::here("inputs/conceptSets")}.}

\item{dryRun}{Logical. When \code{TRUE}, report what would change without writing.
Defaults to \code{FALSE}.}
}
\value{
Invisibly, a tibble with columns \code{id}, \code{old_path}, \code{new_path},
\code{status} (one of \code{"rewritten"}, \code{"would_rewrite"}, \code{"unchanged"},
\code{"broken"}, \code{"no_path"}).
}
\description{
The concept-set counterpart of \code{\link[=normalizeCohortManifestPaths]{normalizeCohortManifestPaths()}}. Rewrites the
\code{file_path} of every active row in \code{conceptSetManifest.sqlite} so it is stored
relative to the study repository root. Only \code{file_path} changes — hashes,
\code{status} and timestamps are untouched, so this never registers as a
definition change.
}
\seealso{
\code{\link[=findStudyProjectRoot]{findStudyProjectRoot()}}, \code{\link[=loadConceptSetManifest]{loadConceptSetManifest()}}
}
