% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/migrateConceptSetManifest.R
\name{migrateConceptSetManifest}
\alias{migrateConceptSetManifest}
\title{Migrate Old ConceptSetManifest SQLite to New Schema}
\usage{
migrateConceptSetManifest(
  dbPath = "inputs/conceptSets/conceptSetManifest.sqlite",
  categoryMap = NULL
)
}
\arguments{
\item{dbPath}{Character. Path to the existing \code{conceptSetManifest.sqlite} file.
Defaults to \code{"inputs/conceptSets/conceptSetManifest.sqlite"}.}

\item{categoryMap}{Named list. Maps concept set labels to categories.
Example: \code{list("BMI Ratio" = "measurement", "Diabetes" = "condition_occurrence")}.
Concept sets not in the map will be assigned from tag keywords or "init".}
}
\value{
Invisible tibble of migrated rows with their assigned categories.
}
\description{
One-time migration utility that converts an existing \code{conceptSetManifest.sqlite}
(picard <= 0.0.3.1) to the new schema with \code{category}, \code{status}, \code{deleted_at} columns,
and converts tags from pipe-delimited string format to JSON.
}
\details{
The migration performs the following steps:
\enumerate{
\item Backs up the old database to \verb{conceptSetManifest_backup_\{timestamp\}.sqlite}
\item Reads all rows from the old schema
\item Assigns \code{category} from the tag keywords (e.g., "domain: condition_occurrence" -> "condition_occurrence")
or defaults to "init" if not found
\item Converts tags from pipe-delimited string (e.g., "name: value | name: value") to JSON named list
\item Adds missing columns (category, status, deleted_at) if they don't exist
\item Creates new schema with proper structure
\item Inserts migrated rows
\item Prints migration summary
}
}
