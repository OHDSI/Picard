% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest_helpers.R
\name{plotCohortGraph}
\alias{plotCohortGraph}
\title{Plot Cohort Dependency Graph}
\usage{
plotCohortGraph(manifest)
}
\arguments{
\item{manifest}{A \link{CohortManifest} R6 object.}
}
\value{
Character. The mermaid diagram string (invisibly).
}
\description{
Generates a mermaid \verb{graph TD} diagram showing how cohorts in a
\link{CohortManifest} depend on each other. Dependency data is read directly from
the SQLite manifest database — no sidecar JSON files required.

Prints the mermaid string to the console (renders automatically in RStudio,
Quarto, and GitHub markdown) and returns it invisibly.
}
\details{
\strong{Node shapes by cohort type:}
\itemize{
\item Rectangle \verb{[label]} — circe (base) cohort
\item Circle \code{(label)} — subset cohort
\item Diamond \code{{{label}}} — union cohort
\item Hexagon \code{{{{{label}}}}} — complement / composite cohort
}

Arrows show dependency direction: parent → dependent cohort.

For a tabular view of derived cohorts and their rule parameters, see
\link{CohortManifest}\verb{$reviewDependentCohorts()}.
}
