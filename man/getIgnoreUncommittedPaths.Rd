% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{getIgnoreUncommittedPaths}
\alias{getIgnoreUncommittedPaths}
\title{Read Configured Code-State Ignore Paths}
\usage{
getIgnoreUncommittedPaths(configFilePath = "config.yml")
}
\arguments{
\item{configFilePath}{Character. Path to config.yml. Defaults to
"config.yml" in the working directory.}
}
\value{
Character vector of paths. \code{character(0)} when the file is absent,
unparseable, or the setting is not present — so the default is to ignore
nothing.
}
\description{
Reads \code{ignoreUncommittedPaths} from the study's config.yml. The
setting lives in the \verb{default:} block (alongside \code{projectName} and
\code{version}) and lists repo-relative paths whose uncommitted changes should
not fail the "Code state" pre-flight check.

Putting the list in config.yml keeps it version-controlled and reviewable:
the same incidental churn recurs on every run, so it is a property of the
study, not of one invocation.
}
\examples{
\dontrun{
# config.yml
# default:
#   projectName: myStudy
#   version: 1.0.0
#   ignoreUncommittedPaths:
#     - inputs
getIgnoreUncommittedPaths()
}
}
\keyword{internal}
