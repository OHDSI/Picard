% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest_helpers.R
\name{expandManifestTags}
\alias{expandManifestTags}
\title{Expand JSON Tags to Columns}
\usage{
expandManifestTags(df, dropTagsCol = TRUE)
}
\arguments{
\item{df}{Data frame. A manifest output dataframe with a \code{tags} column containing
JSON strings (e.g., from \code{ConceptSetManifest$tabulateManifest()} or
\code{ConceptSetManifest$queryConceptSetsByTag()}).}

\item{dropTagsCol}{Logical. If TRUE, drops the original \code{tags} column after expansion.
Defaults to TRUE.}
}
\value{
Data frame. The input dataframe with JSON tags expanded into separate columns.
Rows with NA tags are preserved with NA values in the new columns.
}
\description{
Takes a manifest dataframe (from \code{tabulateManifest()}, \code{queryConceptSetsByTag()}, etc.)
and pivots the JSON tags column into separate columns. Each tag key becomes a column name,
and the values populate the rows.
}
\details{
This function:
\enumerate{
\item Parses each JSON string in the tags column using \code{jsonlite::fromJSON()}
\item Extracts all unique keys across all JSON objects
\item Creates new columns for each key
\item Populates values, with NA for missing keys in any row
\item Optionally drops the original JSON tags column
}

\strong{Example:}
Input dataframe:

\if{html}{\out{<div class="sourceCode">}}\preformatted{id | label | tags
1  | CS1   | \{"category":"drug","subCategory":"steroid","domain":"drug_exposure"\}
2  | CS2   | \{"category":"covariate","domain":"condition_occurrence"\}
}\if{html}{\out{</div>}}

Output dataframe:

\if{html}{\out{<div class="sourceCode">}}\preformatted{id | label | category  | subCategory | domain
1  | CS1   | drug      | steroid     | drug_exposure
2  | CS2   | covariate | NA          | condition_occurrence
}\if{html}{\out{</div>}}
}
