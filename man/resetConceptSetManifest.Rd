% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest_helpers.R
\name{resetConceptSetManifest}
\alias{resetConceptSetManifest}
\title{Reset Concept Set Manifest}
\usage{
resetConceptSetManifest(
  manifest = NULL,
  conceptSetsFolderPath = here::here("inputs/conceptSets"),
  scope = c("manifest", "full"),
  archive = TRUE,
  confirm = TRUE
)
}
\arguments{
\item{manifest}{A \code{ConceptSetManifest} R6 object. Optional; when
provided the folder path is inferred automatically.}

\item{conceptSetsFolderPath}{Character. Path to the conceptSets folder.
Inferred from \code{manifest} when provided; otherwise defaults to
\code{here::here("inputs/conceptSets")}.}

\item{scope}{Character. One of \code{"manifest"} (default) or
\code{"full"}.}

\item{archive}{Logical. For \code{scope = "manifest"}, if \code{TRUE} (default),
archives JSON files in \code{json/} to a timestamped directory instead of
deleting them. Set to \code{FALSE} to delete without archiving.}

\item{confirm}{Logical. If \code{TRUE} (default), the user must type
\code{"yes"} to proceed. Set to \code{FALSE} for non-interactive use.}
}
\value{
Invisibly returns NULL.
}
\description{
Cleans up concept set manifest data at one of two scopes. All destructive
operations require the user to type \code{"yes"} at a confirmation prompt
(disable with \code{confirm = FALSE} for scripted use).
}
\details{
Unlike cohort manifests, all concept set JSON files are user-owned sources
(nothing is auto-generated), so there is no "derived" tier.
}
\section{Scope options}{

\describe{
\item{\code{"manifest"} (default)}{Deletes only the SQLite database.
JSON files in \code{json/} are archived to a timestamped directory
(unless \code{archive = FALSE}) and can be restored after
\code{initConceptSetManifest()} using \code{$addConceptSetFile()}.}
\item{\code{"full"}}{Deletes the SQLite database, the \code{json/}
folder, and \code{conceptSetsLoad.csv}. Complete wipe.}
}
}

