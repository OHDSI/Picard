% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/migrateCohortManifest.R
\name{migrateCohortManifest}
\alias{migrateCohortManifest}
\title{Migrate Old CohortManifest SQLite to New Schema}
\usage{
migrateCohortManifest(
  dbPath = "inputs/cohorts/cohortManifest.sqlite",
  categoryMap = NULL
)
}
\arguments{
\item{dbPath}{Character. Path to the existing \code{cohortManifest.sqlite} file.
Defaults to \code{"inputs/cohorts/cohortManifest.sqlite"}.}

\item{categoryMap}{Named list. Maps cohort labels to categories.
Example: \code{list("Type 2 Diabetes" = "target", "GI Bleed" = "outcome")}.
Cohorts not in the map will be assigned from tag keywords or "unclassified".}
}
\value{
Invisible tibble of migrated rows with their assigned categories.
}
\description{
One-time migration utility that converts an existing \code{cohortManifest.sqlite}
(picard <= 0.0.3) to the new schema with \code{category}, \code{source_type}, \code{cohort_type},
\code{depends_on}, \code{dependency_rule}, and timestamp columns.
}
\details{
The migration performs the following steps:
\enumerate{
\item Backs up the old database to \verb{cohortManifest_backup_\{timestamp\}.sqlite}
\item Reads all rows from the old schema
\item Infers \code{source_type} from file path (\verb{json/} -> "circe", \verb{sql/} -> "sql", \verb{derived/} -> "derived")
\item Infers \code{cohort_type} from old \code{cohortType} column
\item Assigns \code{category} from \code{categoryMap}, tag keywords, or defaults to "unclassified"
\item Converts tags from pipe-delimited string to JSON named list
\item Reads sidecar \code{.json} metadata for dependent cohorts -> \code{dependency_rule} and \code{depends_on}
\item Creates new schema with unique indexes
\item Inserts migrated rows
\item Prints migration summary
}
}
