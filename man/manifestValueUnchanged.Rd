% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest_helpers.R
\name{manifestValueUnchanged}
\alias{manifestValueUnchanged}
\title{Test whether a candidate manifest column value differs from the stored one}
\usage{
manifestValueUnchanged(newValue, storedValue)
}
\arguments{
\item{newValue}{The value about to be written. \code{NULL}, zero-length, and \code{NA}
are all treated as "no value".}

\item{storedValue}{The value currently held in the manifest row.}
}
\value{
Logical. \code{TRUE} when the stored value already matches.
}
\description{
SQLite rewrites the pages it touches, so an \code{UPDATE} that assigns the value a
row already holds still changes the manifest file on disk (and bumps
\code{updated_at}), which surfaces as an uncommitted change in git. Manifest
writers use this to drop no-op assignments before issuing an \code{UPDATE}.
}
\keyword{internal}
